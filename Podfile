# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Flixster(iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Flixster(iOS)
   pod "AlamofireImage"

  target 'Flixster(iOS)Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Flixster(iOS)UITests' do
    # Pods for testing
  end

end
